.DS_Store
# EasyListing.xcodeproj is generated by XcodeGen but committed anyway:
# Xcode Cloud validates the workflow's project reference before it runs
# ci_post_clone.sh, so a repo without it can't start a build.
**/xcuserdata/
ios/build/
ios/DerivedData/
.vercel
.env*
